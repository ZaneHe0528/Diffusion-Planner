# 开题答辩 PPT 大纲

---

## 第 2 页｜汇报提纲

1. 研究背景与动机
2. 问题定义与研究目的
3. 相关工作与本文定位
4. 总体方案与技术路线
5. 研究内容（一）：Falcon 式 warm-start 基础设施
6. 研究内容（二）：学习型场景门控网络
7. 研究内容（三）：安全机制与评测设计
8. 创新点
9. 研究计划与预期成果
10. 风险与应对

---

# 第一部分：研究背景与动机

---

## 第 3 页｜研究背景（1）：学习型规划器的两大现实挑战

### 挑战一：扩散推理的计算瓶颈（本课题主线）

**要点**

- **扩散规划器**（Diffusion-Planner）在 nuPlan 开环/闭环规划 benchmark 上表现优异，已成为轨迹生成的重要范式。
- **基线推理配置**：DPM-Solver++，10 步采样，**NFE ≈ 11**（函数评估次数）。
- **延迟结构**（已实测）：
  - GPU 推理总延迟 ≈ 21.4 ms  
  - 其中**去噪占 ~90%**（≈ 19.3 ms）  
  - 编码等非去噪部分仅占一小部分
- **闭环设定**：重规划频率 10 Hz（步长 0.1 s），每帧都需完整推理 → 去噪是**实时部署的主要瓶颈**，也是本课题**推理加速**的直接出发点。

**与本研究的关联**

- 帧间轨迹复用（warm-start）可将 NFE 从 ≈11 降至 2–3，实现约 **3–4× 去噪加速**；
- 学习型门控进一步按场景自适应分配 t_s，推动 **NFE–分数帕累托前沿**。

---



### 挑战二：时间不一致性（Temporal Inconsistency）

**问题描述**

> 尽管基于学习的运动规划器近年来取得了显著进展，但它们仍经常受到**时间不一致性**的影响。跨帧的小扰动可能累积成不稳定的轨迹，从而降低闭环驾驶中的**舒适性**与**安全性**。

**具体表现**

- 10 Hz 重规划下，每帧独立采样 → 相邻帧轨迹在重叠时段可能出现**抖动、跳变**；
- 扩散规划器从随机噪声出发完整去噪，帧间**无显式时序约束**，扰动更易逐帧叠加；
- 开环指标优异 ≠ 闭环舒适：乘客可感知的加速度/转向不连续，长尾场景下可能放大为安全隐患。

**与本研究的关联（协同收益）**

- warm-start 以上一帧轨迹为先验锚定当前帧，**顺带缓解**跨帧扰动累积；
- 自适应门控在突发场景回退完整去噪，避免过度复用损害响应性——加速为主、一致性为辅。

**讲稿提示**：**先讲推理瓶颈**（与课题主题「推理加速」对齐），再简要带过时间不一致性作为 warm-start 的**额外动机**；后续指标仍围绕 **去噪 NFE / 去噪延迟** 展开。

**图示建议**：左半（主）——饼图或柱状图（去噪 90%、NFE 11→2–3）；右半（辅）——相邻两帧轨迹重叠段抖动示意。

---



## 第 4 页｜研究背景（2）：帧间轨迹复用（Warm-Start）思路

**核心观察（Falcon 等工作）**

- 相邻重规划帧之间，上一帧去噪得到的**干净轨迹** x_0 与当前帧最优轨迹往往**高度相似**。
- 不必每帧从纯噪声 t=T 完整去噪；可复用上一帧轨迹作为先验。
- **主收益**：减少去噪步数 → 降低 NFE（本课题核心）；
- **协同收益**：以先验轨迹锚定当前帧 → 有助于缓解跨帧时间不一致性。

**Warm-Start 基本流程**

```
上一帧 x₀ → 坐标对齐 + 时移 → 前向加噪到 t_s → 从 t_s 部分去噪 → 当前帧轨迹
```

**加速潜力**


| 模式               | NFE  | 去噪延迟（量级）   |
| ---------------- | ---- | ---------- |
| 完整去噪（基线）         | ≈ 11 | ≈ 19 ms    |
| 部分去噪（warm-start） | 2–3  | ≈ 5 ms     |
| **理论加速**         | —    | **约 3–4×** |


**迁移难点（自动驾驶特有）**

- 自车坐标系每帧变化 → 需 **SE(2) 对齐**  
- 多车联合去噪 → 邻车 **token 匹配 / 外推**  
- 突发场景（急刹、切入）→ 需**安全回退**机制

**图示建议**：两帧叠加的 ego 轨迹示意图，标出「重叠段高度一致」。

---



## 第 5 页｜研究背景（3）：固定复用策略的局限

**现状**：Falcon 式方法多采用**固定重加噪起始水平** t_s（如 0.2 / 0.3 / 0.5 扫参取最优单点）。

**两类失效模式**


| 场景类型                | 固定 t_s 的问题      | 后果                   |
| ------------------- | --------------- | -------------------- |
| **简单场景**（跟停、匀速直行）   | t_s 偏保守，仍做偏多去噪  | 算力浪费，加速不充分           |
| **复杂场景**（急刹、变道、强交互） | t_s 偏激进，参考轨迹偏差大 | 频繁触发事后回退，NFE 与安全性双受损 |


**手工规则的不足**

- 可用 ego 速度 / 加速度 / 曲率 / 距前车等阈值粗分「场景复杂度」；
- 但难以刻画：**「当前帧轨迹复用是否有效」**——这是与扩散模型**内部表征**密切相关的信号，非运动学量能完整表达。

**引出核心问题**  

> 能否**自适应**设定每帧的 t_s，做到「简单帧多复用、复杂帧少复用甚至不复用」？

**图示建议**：二维示意图——横轴场景变化幅度 d，纵轴最优 t_s，固定 t_s 为水平线，最优策略为上升曲线。

---



## 第 6 页｜研究背景（4）：BLUE 的启发

**BLUE 做了什么（Bench2Drive / VLA）**

- 发现：**预训练 VLA 的隐藏状态中已编码「当前帧是否受益于语言生成」的信号**；
- 单靠场景复杂度或车辆运动学特征**难以准确预测**该信号；
- 方案：在**冻结 VLA 主干**上挂 **~0.11M 参数**轻量 gate，逐帧决定是否启用语言生成；
- 标签：来自常规驾驶评测——比较「语言模式」与「直接动作模式」的闭环成功率差异，**无需人工标注**；
- 效果：最优性能 + **约 2.54× 推理加速**，不修改主干。

**本文的类比洞察**


| BLUE                | 本文                                     |
| ------------------- | -------------------------------------- |
| 冻结 VLA hidden state | 冻结 Diffusion-Planner **场景嵌入 encoding** |
| 预测「语言是否有用」          | 预测**帧间轨迹变化距离** d                       |
| 控制是否生成语言            | 控制**重加噪水平** t_s 与去噪步数                  |
| 标签来自闭环模式对比          | 标签来自**离线完整去噪回放**（零额外标注）                |


**核心类比句（可放强调框）**  

> 预训练表征中可能已编码「生成效用」信息；只需轻量 gate 将其映射为逐帧推理决策。

**图示建议**：左右对比表 BLUE vs Ours，中间箭头「范式迁移」。

---



## 第 7 页｜问题定义与研究目的

**问题定义**

在 nuPlan 闭环扩散规划场景下，**推理延迟**（去噪占 GPU 推理 ~90%，NFE≈11）是制约实时部署的首要瓶颈，本课题以此为核心；同时，学习型规划器普遍存在**时间不一致性**（跨帧扰动累积），帧间复用可带来协同收益。如何在不显著损害闭环规划质量的前提下，通过**帧间轨迹复用 + 学习型场景门控**，实现**自适应、可回退**的推理加速？

**研究目的（三层）**


| 层次       | 具体目标                                                                               |
| -------- | ---------------------------------------------------------------------------------- |
| **性能目标** | val14 / test14-hard 上 CLS-NR、CLS-R 相对基线掉幅 **< 1 分**；平均 NFE 显著下降，推动 **NFE–分数帕累托前沿** |
| **科学目标** | 证明场景嵌入（encoding）预测帧间变化 d 及闭环表现**显著优于** rule-based 门控（ego 运动学 / 场景复杂度）              |
| **工程目标** | 交付可复现 warm-start 模块 + ~0.1M 级 gate；**冻结 planner**，训练极轻量，推理开销可忽略                    |


**方法定位调整**

- 原 Falcon 计划：「训练无关」的适配版 warm-start；  
- 演进后：「**训练极轻量**」——冻结主干 + 微型 gate + 离线自动标签。

**图示建议**：三目标三角形或三层金字塔。

---



# 第二部分：相关工作与本文定位

---



## 第 8 页｜相关工作与本文定位

**相关方向**

1. **扩散模型加速**：步数缩减、蒸馏、缓存、流式/warm-start 去噪（Falcon 等，图像/视频领域成熟）
2. **自动驾驶扩散规划**：Diffusion-Planner 等在 nuPlan 的高分表现
3. **自适应推理**：早退、动态深度、条件计算；BLUE 在 VLA 上的逐帧门控
4. **轨迹时序复用**：相邻帧规划结果的相关性、重规划频率与轨迹步长对齐（0.1 s）

**本文差异化贡献（查新要点，M0 验证）**

- 在 **nuPlan 闭环 + 扩散规划器**上系统实现 warm-start；  
- **坐标系对齐 + 邻车联合去噪 + 验证式安全回退**的完整工程方案；  
- 首次（待查新确认）在**冻结场景嵌入**上学 gate，以**帧间变化距离**为标签，**自适应 t_s**；  
- gate 事前分配 + 阈值事后兜底的双路径框架。

**与 Falcon 移植计划的关系**

```
Falcon 移植（固定 t_s）  ──演进──▶  本课题（学习型自适应 t_s）
     │                                      │
     └──── warm-start 基础设施完全复用 ──────┘
```

**图示建议**：相关工作矩阵图（横轴：是否自适应；纵轴：是否驾驶闭环）。

---



# 第三部分：总体方案

---



## 第 9 页｜总体技术路线

**递进式研究路线**

```
M0 查新 → M1 基础设施 + 离线数据 → M-probe 可行性探针
    → M2 固定 t_s 闭环基线 → M3 门控训练 + 集成扫参 → M4 全量评测
```

**单帧推理逻辑（鸟瞰）**

```
┌─────────────────────────────────────────────────────────┐
│  场景观测 → Encoder → encoding（场景嵌入，预去噪即得）      │
│                        ↓                                 │
│              Gate: d_hat = f(encoding)   ← 不计 NFE      │
│                        ↓                                 │
│              分箱映射 → 操作点 (t_s, steps)                 │
│                        ↓                                 │
│  上一帧 x₀ → SE(2)对齐+时移 → 重加噪到 t_s → 部分去噪      │
│                        ↓                                 │
│         一步估计 + 阈值验证 → 通过：少步完成 / 失败：完整去噪  │
└─────────────────────────────────────────────────────────┘
```

**关键设计原则**

- 固定 t_s 版是**关键对照基线**，不是被抛弃的前置工作；  
- Gate 与阈值**两者都保留**：事前主动分配 + 事后验证兜底；  
- 所有模块**配置开关控制**，关掉即恢复官方基线。

**图示建议**：流程框图（本页核心视觉）。

---



## 第 10 页｜与 Falcon / BLUE 方案对照


| 维度         | Falcon 原论文 | Falcon 移植（一期） | 本课题（演进版）                 |
| ---------- | ---------- | ------------- | ------------------------ |
| 应用场景       | 图像/视频生成    | nuPlan 扩散规划   | nuPlan 扩散规划              |
| Warm-start | 有          | 有（重加噪）        | **复用，不变**                |
| t_s 设定     | 固定         | 固定扫参          | **学习型门控自适应**             |
| 安全机制       | 一步估计+阈值    | 一步估计+阈值       | **复用，作安全网**              |
| 邻车处理       | —          | token 匹配+外推   | **复用，不变**                |
| 是否训练       | 无          | 无             | **极轻量 gate ~0.1M**       |
| Gate 输入    | —          | —             | encoding（主）/ ego 运动学（精简） |
| Gate 标签    | —          | —             | 帧间变化距离 d（离线自动）           |


**叙事口径**

- 报告指标：**模型推理（去噪）延迟 / NFE**，非整车端到端延迟；  
- 结论形式：**「分数不降 + NFE 降 N 倍」**；加速天花板约 3–4×（基线仅 11 NFE）。

---



# 第四部分：研究内容

---



## 第 11 页｜研究内容一：Warm-Start 基础设施

**模块清单**


| 模块          | 技术要点                                                                   |
| ----------- | ---------------------------------------------------------------------- |
| **帧间缓存**    | 存储上一帧 ego/邻车最终轨迹（物理空间）、anchor ego state、邻车 token 列表；场景切换时清空            |
| **参考轨迹构造**  | ego x_0 → SE(2) 变换到当前 ego 系 → **时移 1 下标**（末尾匀速补点）；步长 0.1 s = replan 间隔 |
| **邻车 x_0**  | track token 命中 → 复用对齐；未命中 → **匀速外推** 80 步                              |
| **重加噪**     | VPSDE 前向：x_{t_s} = \bar\alpha(t_s)\cdot x_0 + \sigma(t_s)\cdot z       |
| **部分去噪**    | `DPM_Solver.sample(t_start=t_s, steps=缩减步数)`，原生支持                      |
| **首帧 / 失效** | 缓存为空或上帧回退失败 → **完整去噪**（NFE≈11），gate 不参与                                |




---



## 第 13 页｜研究内容二：学习型场景门控——设计概览

**Gate 职责（一句话）**  

> 在冻结 planner 的**场景嵌入**上，回归预测**帧间变化距离程度** \hat{d}，映射为 warm-start **操作点** (t_s, \text{steps})。

**双方案并行（主方案 + 工程精简版）**


|      | **主方案（BLUE 式）**                       | **精简方案（gate_v2）**                             |
| ---- | ------------------------------------- | --------------------------------------------- |
| 输入   | encoding mean-pool（**encoding-only**） | ego 运动学 14 维 + ego_history + 邻车轻量 TokenBranch |
| 动机   | 验证「预训练嵌入 > 手工特征」                      | 已验证 Spearman≈0.64，工程可落地                       |
| 参数量  | ~0.1M（单隐层 MLP）                        | <0.1M（ego ~27K + 邻车 ~45K + trunk ~16K）        |
| 论文叙事 | **核心科学主张**                            | 消融 / 工程对照                                     |


**输出统一**

- 回归 \hat{d}（物理空间，米）→ **分箱映射** → (t_s, \text{steps})；  
- 最高档 = **完整去噪**（主动判难，跳过 warm-start 尝试）。

**图示建议**：Gate 结构图——输入 → MLP → \hat{d} → 分箱 → 操作点。

---



## 第 14 页｜研究内容二（续）：门控网络结构

**主方案结构（encoding-only）**

```
encoding [B, N, D] → mean-pool(valid tokens) → [B, D]
    → Linear(D, 128) → SiLU → Linear(128, 1) → d_hat
```

- 单隐层 MLP，hidden≈128，参数量 ~0.1M，**对齐 BLUE 量级**；  
- 训练：Huber / MSE on **标准化** d；  
- Planner **全程冻结**。

**精简方案结构（RawObsGate 裁剪版）**

- **Ego 分支**：`MLP(21×7 + 14 → 128 → 64)`，14 维运动学含速度、加速度、曲率、偏航角速度等；  
- **邻车分支**：`TokenBranch(32,21,11 → 64)`，仅补 hard 交互帧（AUROC +~0.015）；  
- **Trunk**：`concat → LayerNorm → Linear(128)+SiLU`；  
- **仅回归头** \hat{d}，删分类头；档位由 `bucketize(d_hat, level_edges)` 得到。

**Ego 运动学与场景复杂度对应（精简方案）**


| 场景         | 运动学特征表现                                    |
| ---------- | ------------------------------------------ |
| 静止/跟停（低 d） | speed≈0, accel≈0, yaw_rate≈0, is_stopped=1 |
| 急加速（高 d）   | accel 大, speed_delta 大                     |
| 转弯/变道（高 d） |                                            |


**图示建议**：双分支网络结构图。

---



## 第 15 页｜研究内容二（续）：标签、数据与训练

**标签 d 的定义（与阈值完全一致）**

- 离线逐帧跑**完整去噪**得当前最优轨迹；  
- 与 warm-start 参考轨迹（上帧对齐+时移）在 ego 重叠段算逐点欧氏距离；  
- 取 **max** → 帧间变化距离 d（物理空间，米）。

**训练数据构建**


| 项目   | 规格                                             |
| ---- | ---------------------------------------------- |
| 场景规模 | 数百 ~ 1–2k nuPlan scenario                      |
| 帧配对数 | 数万 `(encoding, d)` 或 `(obs, d)`                |
| 划分   | 按 **scenario** 分 train/val/test，防泄漏            |
| 下采样  | 停车段、相邻帧 cos>0.99 段**冗余下采样**，防静止帧主导             |
| 额外产出 | 参考 vs 完整去噪距离分布图（定 \varepsilon，复刻 Falcon Fig.2） |


**训练配置**

- 损失：标准化 \log(1+d) 或 d 上 Huber；  
- 不触碰 planner 权重；  
- 可选闭环回灌（DAgger 式）作消融。

**图示建议**：距离分布直方图——主体集中在小值区 + 可辨长尾。

---



## 第 16 页｜研究内容二（续）：\hat{d} → (t_s, \text{steps}) 映射

**分档映射（工程落地，边界离线标定）**


| 档位     | d 范围（示例，米）             | t_s   | steps | 策略             |
| ------ | ---------------------- | ----- | ----- | -------------- |
| **低档** | d \lesssim 0.28        | 最小    | ≈2    | 激进复用，最大加速      |
| **中档** | 0.28 < d \lesssim 0.70 | 中等    | ≈3–4  | 平衡             |
| **高档** | 0.70 < d \lesssim 1.39 | 较大    | ≈6–8  | 保守复用           |
| **满档** | d > 1.39               | T（完整） | 10    | **不复用**，主动完整去噪 |


*边界取自全量 normalized-L2 分位：P50=0.275, P75=0.696, P90=1.385*

**备选：噪声调度反解**

- VP 调度：x_{t_s} = \alpha(t_s) x_0 + \sigma(t_s) \epsilon  
- 令 \sigma(t_s) \approx 经 `state_normalizer` 折算后的 \hat{d}，反解 t_s；  
- 步数：\text{steps} = \text{clamp}(\lceil \text{basesteps} \cdot t_s / T \rceil, \text{min}, \text{base})

**扫参范围（小开发集）**

- 操作点集 × \varepsilon × 分箱边界；  
- 观察 **NFE–分数帕累托前沿**是否优于固定 t_s。

**图示建议**：d 轴上的四色分档条 + 对应 NFE 柱状图。

---



## 第 17 页｜研究内容三：双路径安全机制

**为何需要「双路径」？**

- 仅靠 gate：可能**误判**易帧为难帧或反之；  
- 仅靠阈值：只能**事后**发现失败，无法事前省掉无谓 warm-start 尝试；  
- **协同**：事前分配算力 + 事后验证安全。

**路径一：主动判难（Gate 事前）**

- \hat{d} 落入满档 → **直接完整去噪**；  
- 避免对「已知难帧」做无效 warm-start + 回退；  
- 诊断指标：**主动判难率**。

**路径二：被动回退（阈值事后）**

- warm-start 后一步估计得 d_{\text{meas}}；  
- d_{\text{meas}} > \varepsilon → 回退完整去噪（+1 NFE 开销）；  
- 诊断指标：**被动回退率**（预期 <10%，过高说明 \varepsilon 过紧或对齐有 bug）。

**额外保守策略（精简方案）**

- Val 集校准：\hat{d} 阈值保 **hard recall ≥ 95%**；  
- 邻车强交互时**上调一档**，防上下客/避让类突变；  
- 可选：每 N 帧强制完整去噪，防误差累积（消融行）。

**图示建议**：双路径汇合图——主动（c 步）与被动（g 步）均到达「完整去噪」。

---



## 第 20 页｜评测方案

**评测矩阵（M4 全量执行）**


| #   | 配置                                | 目的                                 |
| --- | --------------------------------- | ---------------------------------- |
| 1   | 基线 DPM-Solver 10 步（NFE≈11）        | 官方分数锚点                             |
| 2   | 直接降步数 2/3/5 步（无 warm-start）       | 同 NFE 预算下 warm-start 的价值           |
| 3   | 固定 t_s warm-start（Falcon phase-1） | **关键对照**：自适应的增量                    |
| 4   | rule-based gate → t_s             | 证 encoding > 手工特征（类比 BLUE Table 7） |
| 5   | **场景门控自适应 t_s（本方法）**              | **主结果**                            |
| ＋   | 朴素 warm-start（无阈值回退）              | 消融：安全网必要性                          |




---



# 第五部分：创新点

---



## 第 21 页｜创新点（1）：nuPlan 闭环扩散 warm-start 系统适配

**创新概述**  
首次在 nuPlan 闭环场景下，为扩散规划器构建**可开关、可回退**的帧间轨迹 warm-start 完整链路，实现**推理加速**（NFE 3–4× 量级）；同时 warm-start 对跨帧时间一致性具有协同改善作用。

**针对自动驾驶的专项设计**

1. **物理空间 SE(2) 对齐 + 时移**——解决每帧 ego 坐标系变化，保证复用轨迹几何一致；
2. **邻车 track token 匹配 + 匀速外推**——多车联合去噪的帧间一致性；
3. **一步估计 + 阈值回退**——验证式安全网，突发场景下主动放弃复用、守住 test14-hard 等挑战集；
4. **诚实 NFE 记账**——一步估计开销不隐藏。

**独立价值**  
即使自适应 gate 未达预期，固定 t_s + 安全网仍是相对「直接降步数」的显著增益，构成**可独立发表/交付的基础贡献**。

---



## 第 22 页｜创新点（2）：BLUE 式场景嵌入门控

**核心命题**  

> 扩散规划器冻结的**场景嵌入**已编码帧间轨迹变化幅度；~0.1M 级 MLP 足以将其映射为**自适应 t_s 决策**。

**与 BLUE 的范式对齐**


| 属性      | 本文体现                          |
| ------- | ----------------------------- |
| 冻结主干    | Planner encoder 权重不变          |
| 轻量 gate | ~0.1M，单隐层 MLP                 |
| 无人工标注   | 标签 d 由离线完整去噪回放自动生成            |
| 逐帧决策    | 每帧独立预测 \hat{d} → 操作点          |
| 科学论证    | encoding **vs** rule-based 消融 |


**区别于简单「场景复杂度分类」**  
Gate 直接回归与阈值**同口径**的连续距离 d，而非离散「简单/复杂」标签 → 与 warm-start 机制**天然对齐**。

---



## 第 23 页｜创新点（3）：双路径自适应推理框架

**创新概述**  
提出 gate **事前分配**与阈值**事后验证**的协同机制，而非二选一。

```
                    ┌─ 主动判难 → 完整去噪（省无效 warm-start）
  Gate 预测 d_hat ──┤
                    └─ 低档/中档/高档 → warm-start → 阈值验证
                                              ├─ 通过 → 少步完成
                                              └─ 失败 → 被动回退
```

**理论与实用价值**

- 覆盖「可预测变化」与「不可预测突发」两类情形；  
- 支持主动/被动率分开诊断、闭环数据回灌；  
- 在平均 NFE 与闭环安全性之间取得**更优帕累托权衡**。

---



## 第 24 页｜创新点（4）：零标注标签 + 风险可控研究范式

**离线标签构造**

- d 的定义与推理阈值**完全一致** → 训练目标与部署行为对齐，无口径漂移；  
- 复用 M1 离线回放管线，**零额外标注成本**。

**M-probe 门槛性验证**

- 先证「嵌入可预测 d」→ 再投入 gate 训练与闭环集成；  
- 失败止损路径明确：退回固定 t_s 基线，研究风险可控。

**对比传统做法**


| 传统             | 本文                     |
| -------------- | ---------------------- |
| 先实现完整系统再调参     | 离线分布 → 探针 → 闭环，三层金字塔验证 |
| 自适应 t_s 作「加分项」 | 自适应 t_s 提升为**核心贡献**    |
| 隐藏失败风险         | 明确止损条件与可交付下限           |


---





---



## 第 28 页｜总结

**一句话总结**  

> 针对扩散规划器的**推理延迟**瓶颈，在冻结 Diffusion-Planner 上以 Falcon 帧间复用降低 NFE，以 BLUE 式轻量 gate 自适应 t_s，以双路径安全机制守住闭环分数；warm-start 同时有助于缓解跨帧时间不一致性。

**三个关键词**  
`推理加速` · `Warm-Start` · `自适应门控`

**核心主张**  

1. 去噪占推理 ~90%，NFE 从 11 降至 2–3 是本课题主目标；
2. 场景嵌入编码帧间变化效用 → 可回归预测 d，自适应分配 t_s；
3. 训练极轻量、无人工标注、风险可控；时间一致性为 warm-start 的协同收益。

**下一步工作**  
M1 基础设施 → M-probe 探针 → 固定 t_s 闭环 → 门控集成 → 全量评测

---

