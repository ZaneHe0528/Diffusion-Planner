# 开题答辩总体框架图

> **课题**：基于学习型场景门控的扩散规划器自适应轨迹复用加速  
> **平台**：Diffusion-Planner + nuPlan 闭环仿真  
> **核心主线**：推理加速（NFE 11 → 2–3）；时间一致性为 warm-start 协同收益

---

## 图 1｜课题总体框架（三层结构）

```mermaid
flowchart TB
    subgraph PROBLEM["问题层"]
        P1["挑战一（主线）<br/>扩散推理瓶颈<br/>NFE≈11 · 去噪占~90%"]
        P2["挑战二（协同）<br/>时间不一致性<br/>跨帧扰动累积"]
        P3["固定 t_s 局限<br/>简单场景浪费算力 · 复杂场景频繁回退"]
    end

    subgraph METHOD["方法层"]
        direction TB
        M0["冻结 Diffusion-Planner 主干"]
        M1["Falcon 式 Warm-Start 基础设施<br/>缓存 · SE(2)对齐 · 重加噪 · 部分去噪"]
        M2["BLUE 式轻量场景门控 ~0.1M<br/>预测帧间变化距离 d_hat → 自适应 t_s"]
        M3["双路径安全机制<br/>主动判难 + 阈值被动回退"]
        M0 --> M1 --> M2 --> M3
    end

    subgraph OUTPUT["验证层"]
        O1["闭环分数 CLS-NR / CLS-R<br/>掉幅 < 1 分"]
        O2["平均 NFE / 去噪延迟<br/>目标 3–4× 加速"]
        O3["NFE–分数帕累托前沿<br/>优于固定 t_s 与 rule-based"]
    end

    P1 --> METHOD
    P2 -.协同收益.-> M1
    P3 --> M2
    METHOD --> OUTPUT

    style P1 fill:#ffcccc,stroke:#cc0000
    style M2 fill:#cce5ff,stroke:#0066cc
    style O2 fill:#ccffcc,stroke:#009900
```

**读图说明**

| 层级 | 内容 |
|---|---|
| 问题层 | 推理延迟是主线动机；时间不一致性是 warm-start 的额外收益；固定 \(t_s\) 引出自适应门控需求 |
| 方法层 | 冻结主干 → warm-start 基础设施 → 学习型门控 → 双路径安全网，四层递进 |
| 验证层 | 闭环分数、NFE/延迟、帕累托前沿三维验收 |

---

## 图 2｜单帧推理总体框架（核心方法图）

```mermaid
flowchart TD
    START(["当前帧 t 到达<br/>10 Hz 重规划"]) --> INPUT

    subgraph INPUT["① 感知与编码"]
        OBS["场景观测<br/>ego / 邻车 / 地图"]
        ENC["Encoder 前向<br/>（冻结 planner）"]
        EMB["场景嵌入 encoding<br/>预去噪即得 · 不计 NFE"]
        OBS --> ENC --> EMB
    end

    INPUT --> CHECK

    CHECK{"② 首帧？<br/>或上帧回退失败？"}
    CHECK -->|是| FULL1["完整去噪<br/>NFE ≈ 11"]
    CHECK -->|否| GATE

    subgraph GATE["③ 学习型场景门控（事前）"]
        GIN["Gate 输入<br/>encoding mean-pool<br/>或 ego运动学+邻车"]
        GHAT["回归 d_hat<br/>帧间变化距离（米）"]
        MAP["分箱映射<br/>d_hat → (t_s, steps)"]
        GIN --> GHAT --> MAP
    end

    MAP --> HARD{"d_hat 满档？<br/>主动判难"}
    HARD -->|是| FULL2["完整去噪<br/>NFE ≈ 11"]
    HARD -->|否| WS

    subgraph WS["④ Warm-Start 轨迹复用"]
        CACHE["读取缓存 x₀_prev"]
        ALIGN["SE(2) 对齐 + 时移 1 步<br/>邻车 token 匹配 / 匀速外推"]
        RENOISE["前向加噪到 t_s<br/>x_ts = α·x₀ + σ·ε"]
        CACHE --> ALIGN --> RENOISE
    end

    WS --> ONESTEP

    subgraph ONESTEP["⑤ 一步估计 + 阈值验证（事后）"]
        DIT["DiT 前向(x_ts, t_s)<br/>→ x̂₀"]
        THRESH{"max‖x̂₀_ego − ref_ego‖ ≤ ε ?"}
        DIT --> THRESH
    end

    THRESH -->|通过| PARTIAL["从 t_s 少步去噪<br/>NFE ≈ 2–3<br/>复用一步估计 · 省 1 NFE"]
    THRESH -->|失败| FULL3["被动回退完整去噪<br/>NFE ≈ 12"]

    FULL1 --> OUT
    FULL2 --> OUT
    PARTIAL --> OUT
    FULL3 --> OUT

    subgraph OUT["⑥ 输出与更新"]
        TRAJ["输出当前帧轨迹"]
        UPD["更新缓存<br/>ego/邻车 x₀ · anchor · tokens"]
        LOG["记录 NFE · d_hat · d_meas<br/>主动/被动标志"]
        TRAJ --> UPD --> LOG
    end

    style GATE fill:#e8f4fd,stroke:#2196F3
    style WS fill:#fff3e0,stroke:#FF9800
    style ONESTEP fill:#fce4ec,stroke:#E91E63
    style PARTIAL fill:#e8f5e9,stroke:#4CAF50
    style FULL1 fill:#ffebee,stroke:#f44336
    style FULL2 fill:#ffebee,stroke:#f44336
    style FULL3 fill:#ffebee,stroke:#f44336
```

**三条到达「完整去噪」的路径**

| 路径 | 触发条件 | 性质 |
|---|---|---|
| 路径 A | 首帧 / 上帧回退失败 | 正常流程 |
| 路径 B | Gate 主动判难（\(d\) 满档） | **事前**跳过无效 warm-start |
| 路径 C | 阈值验证失败 | **事后**安全网被动回退 |

---

## 图 3｜离线训练与在线推理双管线

```mermaid
flowchart LR
    subgraph OFFLINE["离线阶段（训练 Gate）"]
        direction TB
        O1["nuPlan 场景回放<br/>数百~2k scenarios"]
        O2["逐帧完整去噪<br/>（冻结 planner）"]
        O3["构造参考轨迹<br/>上帧 x₀ 对齐+时移"]
        O4["计算标签 d<br/>ego重叠段 max 欧氏距离"]
        O5["缓存配对<br/>(encoding, d)"]
        O6["M-probe 可行性探针<br/>encoding vs 运动学"]
        O7["训练 Gate ~0.1M<br/>Huber/MSE · planner冻结"]
        O1 --> O2 --> O3 --> O4 --> O5 --> O6 --> O7
    end

    subgraph ONLINE["在线阶段（闭环推理）"]
        direction TB
        N1["实时场景观测"]
        N2["Encoder → encoding"]
        N3["Gate → d_hat → (t_s, steps)"]
        N4["Warm-Start + 部分去噪"]
        N5["阈值安全网"]
        N6["输出轨迹 · 更新缓存"]
        N1 --> N2 --> N3 --> N4 --> N5 --> N6
    end

    O7 -.权重加载.-> N3
    O4 -.标签口径=阈值口径 ε.-> N5

    style OFFLINE fill:#f3e5f5,stroke:#9C27B0
    style ONLINE fill:#e3f2fd,stroke:#1976D2
```

---

## 图 4｜与 Falcon / BLUE 的方法融合关系

```mermaid
flowchart LR
    subgraph FALCON["Falcon 范式"]
        F1["帧间 x₀ 复用"]
        F2["重加噪到 t_s"]
        F3["部分去噪"]
        F4["一步估计 + 阈值回退"]
        F1 --> F2 --> F3 --> F4
    end

    subgraph BLUE["BLUE 范式"]
        B1["冻结预训练主干"]
        B2["隐藏状态/嵌入"]
        B3["轻量 Gate ~0.1M"]
        B4["逐帧自适应决策"]
        B1 --> B2 --> B3 --> B4
    end

    subgraph OURS["本课题融合"]
        O1["冻结 Diffusion-Planner"]
        O2["场景嵌入 encoding"]
        O3["Gate 回归 d_hat"]
        O4["自适应 t_s + steps"]
        O5["nuPlan 闭环适配<br/>SE(2) · 邻车 · 安全网"]
        O1 --> O2 --> O3 --> O4 --> O5
    end

    FALCON -->|"warm-start 基础设施<br/>完全复用"| OURS
    BLUE -->|"门控范式迁移<br/>encoding → d → t_s"| OURS

    style OURS fill:#fff9c4,stroke:#F9A825,stroke-width:2px
```

---

## 图 5｜研究技术路线与里程碑

```mermaid
flowchart LR
    M0["M0 查新<br/>半天"] --> M1
    M1["M1 基础设施<br/>+ 离线回放<br/>warm_start.py<br/>(encoding,d)数据集"]
    --> MPROBE
    MPROBE["M-probe 探针<br/>encoding 预测 d ?"]
    MPROBE -->|通过| M2
    MPROBE -->|不通过| FALLBACK
  FALLBACK["止损：固定 t_s<br/>Falcon phase-1"]
    M2["M2 固定 t_s<br/>闭环冒烟<br/>20-30 场景"]
    --> M3
    M3["M3 训 Gate<br/>集成扫参<br/>帕累托前沿"]
    --> M4
    M4["M4 全量评测<br/>val14 + test14-hard<br/>消融 + 写作素材"]

    style MPROBE fill:#fff3e0,stroke:#FF9800
    style M4 fill:#e8f5e9,stroke:#4CAF50
    style FALLBACK fill:#ffebee,stroke:#f44336
```

---

## 图 6｜评测矩阵总览

```mermaid
flowchart TB
    BASE["基线<br/>DPM-Solver 10步<br/>NFE≈11"] 

    subgraph COMPARE["对照组"]
        C1["直接降步数<br/>无 warm-start"]
        C2["固定 t_s warm-start<br/>Falcon phase-1"]
        C3["rule-based gate<br/>运动学阈值"]
    end

    subgraph MAIN["主方法"]
        M["场景门控自适应 t_s<br/>本课题"]
    end

    subgraph ABLATION["消融"]
        A1["朴素 warm-start<br/>无阈值回退"]
        A2["encoding-only<br/>vs ego运动学"]
        A3["邻车分支 开/关"]
    end

    BASE --> COMPARE
    BASE --> MAIN
    MAIN --> ABLATION

    METRICS["汇报指标<br/>CLS-NR · CLS-R<br/>平均NFE · 延迟<br/>主动判难率 · 被动回退率<br/>gate回归误差"]

    COMPARE --> METRICS
    MAIN --> METRICS
    ABLATION --> METRICS

    style MAIN fill:#cce5ff,stroke:#0066cc,stroke-width:2px
    style METRICS fill:#f5f5f5,stroke:#666
```

---

## 图 7｜ASCII 精简版（可直接贴入 PPT）

```
╔══════════════════════════════════════════════════════════════════════════╗
║          基于学习型场景门控的扩散规划器自适应轨迹复用加速                      ║
╠══════════════════════════════════════════════════════════════════════════╣
║  【问题】  推理瓶颈(NFE≈11)  +  时间不一致性  +  固定t_s失效               ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                          ║
║   场景观测 ──► Encoder(冻结) ──► encoding ──► Gate(~0.1M)              ║
║                                      │              │                    ║
║                                      │         d_hat │ 分箱              ║
║                                      │              ▼                    ║
║   缓存 x₀_prev ──► SE(2)对齐+时移 ──► 重加噪到t_s ──► 部分去噪           ║
║        ▲                                    │                            ║
║        │                              一步估计+阈值ε                       ║
║        │                           ┌────────┴────────┐                   ║
║        │                           ▼                 ▼                   ║
║        │                      通过(NFE≈2-3)    回退(NFE≈11)             ║
║        └──────── 更新缓存 ◄──── 输出轨迹                                  ║
║                                                                          ║
╠══════════════════════════════════════════════════════════════════════════╣
║  【训练】  离线回放 → (encoding,d) → M-probe → 训Gate（planner冻结）       ║
║  【验证】  val14/test14-hard · CLS-NR/R · NFE/延迟 · 帕累托前沿           ║
╚══════════════════════════════════════════════════════════════════════════╝
```

---

## 图例与关键符号

| 符号 | 含义 |
|---|---|
| \(x_0\) | 去噪后的干净轨迹 |
| \(t_s\) | 重加噪起始时刻（噪声水平） |
| \(d\) / \(\hat{d}\) | 帧间变化距离（标签 / 预测值，物理空间米） |
| \(\varepsilon\) | 阈值回退判定边界 |
| NFE | 神经网络函数评估次数（去噪步数 + 一步估计） |
| encoding | Encoder fusion 输出的场景嵌入 |
| Gate | 单隐层 MLP，约 0.1M 参数，推理不计 NFE |

---

## 使用建议

| 场景 | 推荐图示 |
|---|---|
| 开题答辩开场（30 秒鸟瞰） | **图 1** 三层结构 |
| 方法核心讲解（2–3 分钟） | **图 2** 单帧推理流程 |
| 解释「如何训练 Gate」 | **图 3** 离线/在线双管线 |
| 说明与 Falcon/BLUE 关系 | **图 4** 融合关系 |
| 研究计划汇报 | **图 5** 里程碑 |
| 实验设计汇报 | **图 6** 评测矩阵 |
| PPT 不便渲染 Mermaid 时 | **图 7** ASCII 版 |

> Mermaid 图可在 VS Code、Typora、GitHub 或 [mermaid.live](https://mermaid.live) 中预览与导出 PNG/SVG。
