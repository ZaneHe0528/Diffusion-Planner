1. **进一步地，作者发现：预训练 VLA 的隐藏状态中可能已经编码了“当前帧是否会受益于语言”的信号；而单靠场景复杂度或车辆运动学特征很难预测这一点。**
	**基于这一发现，BLUE 在冻结的 VLA 隐藏状态上训练一个轻量级门控网络，用于逐帧决定是否激活语言生成，或者直接预测动作。该方法不修改主干网络，也不需要额外人工标注。**


2. 论文在bench2drive里只用了一个camera？


3. 于是本文提出 **BLUE**：在冻结的 VLA 隐藏状态上训练一个轻量级 gate，逐帧判断是否启用语言生成。BLUE 不修改主干，也不需要额外人工标注。训练标签来自常规驾驶评测结果，即比较“语言模式”和“直接动作模式”的闭环成功率差异。


4. ”发现预训练 VLA 隐藏状态中可能已经编码语言效用信号。只需在冻结隐藏状态上训练 0.11M 参数 gate，BLUE 就能在 Bench2Drive 和 Longest6 v2 上取得最优性能，并实现 2.54 倍推理加速。“

那我也发现了 扩散模型隐藏状态可能已经编码了 扩散生成效用信息

设计了一个门控
“Gate 是一个单隐层 MLP，参数量很小，用二元交叉熵训练。**该设计足以把预训练隐藏状态映射为二值门控决策**，同时降低过拟合风险，并保持推理开销近似可忽略。”

扩散模型生成轨迹好像没有可以使用的隐藏状态


5. 本文使用的标签是 作者统计的语言输入对轨迹生成是否有用 ，训练了一个轻量的gate ，用来控制当前帧的推理是否使用语言信息

/grill-with-docs @0Falcon/移植计划.md 这个是我之前准备使用falcon的帧间复用移植计划。
我最近看到了BLUE这篇文章@0BLUE 。有一个新的想法：用统计出来相邻帧轨迹的归一化欧氏距离作为标签，设计一个门控神经网络，让这个gate学习场景特征与相邻帧轨迹归一化欧氏距离的关系，自适应控制复用轨迹后加噪的强度，从而实现自适应轨迹复用。